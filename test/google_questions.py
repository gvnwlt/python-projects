import time
import sys # sys.getsizeof()

# find first repeating characters 

chars1 = "abca"
chars2 = "bcaba"
chars3 = "abc"

def first_repeat(chars):
	arr_size = getsizeof(chars) / getsizeof(chars[0])

	for x in range(len(chars)):
		if chars[x] >= 0:
			chars[x] = -
		else:
			r_chars = chars[x]
	return r_chars

print(first_repeat(chars1))

time.sleep(5)

sizeof char = 1 
sizeof int = 4